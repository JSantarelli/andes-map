import { OrganizacionPipe } from './organizacion.pipe';

describe('OrganizacionPipe', () => {
  it('create an instance', () => {
    const pipe = new OrganizacionPipe();
    expect(pipe).toBeTruthy();
  });
});
